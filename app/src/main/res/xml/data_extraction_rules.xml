<?xml version="1.0" encoding="utf-8"?>

<!--
    Athena data extraction rules.

    Athena does not allow app-private data to be copied
    through cloud backup or device-to-device transfer.

    Vault backups, when implemented, are handled explicitly
    by Athena using the encrypted vault file itself.
-->
<data-extraction-rules>

    <!-- =========================================================
         CLOUD BACKUP
         ========================================================= -->

    <cloud-backup>

        <exclude
            domain="file"
            path="." />

        <exclude
            domain="database"
            path="." />

        <exclude
            domain="sharedpref"
            path="." />

        <exclude
            domain="root"
            path="." />

        <exclude
            domain="external"
            path="." />

    </cloud-backup>

    <!-- =========================================================
         DEVICE TRANSFER
         ========================================================= -->

    <device-transfer>

        <exclude
            domain="file"
            path="." />

        <exclude
            domain="database"
            path="." />

        <exclude
            domain="sharedpref"
            path="." />

        <exclude
            domain="root"
            path="." />

        <exclude
            domain="external"
            path="." />

    </device-transfer>

</data-extraction-rules>