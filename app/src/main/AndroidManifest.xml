<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Athena does not request INTERNET access.

        Biometric authentication is handled through Android's
        biometric APIs / AndroidX Biometric.
    -->
    <uses-permission
        android:name="android.permission.USE_BIOMETRIC" />

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/athena_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/athena_logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Athena">

        <!-- =====================================================
             LAUNCHER
             ===================================================== -->

        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Athena.Splash">

            <intent-filter>

                <action
                    android:name="android.intent.action.MAIN" />

                <category
                    android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <!-- =====================================================
             INTERNAL ACTIVITIES
             ===================================================== -->

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Athena" />

        <activity
            android:name=".VaultActivity"
            android:exported="false"
            android:label="@string/title_activity_vault"
            android:theme="@style/Theme.Athena" />

        <activity
            android:name=".EntryDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_entry_detail"
            android:theme="@style/Theme.Athena" />

        <activity
            android:name=".AddEntryActivity"
            android:exported="false"
            android:label="@string/title_activity_add_entry"
            android:theme="@style/Theme.Athena" />

        <activity
            android:name=".EditEntryActivity"
            android:exported="false"
            android:theme="@style/Theme.Athena" />

        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.Athena" />

        <!-- =====================================================
             FONT PRELOADING
             ===================================================== -->

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>